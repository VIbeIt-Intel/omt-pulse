/** Shared full-width shell for Control Room modules on PC / tablet. */
export const OPS_PAGE_SHELL =
  "w-full max-w-none px-4 sm:px-6 lg:px-8 xl:px-10";

/** Same shell without horizontal padding (when the parent already pads). */
export const OPS_PAGE_CONTENT = "w-full max-w-none";
